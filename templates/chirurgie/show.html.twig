{% extends 'base.html.twig' %}

{% block title %}Chirurgie{% endblock %}

{% block body %}
    <h1>Chirurgie</h1>

    <table class="table" border="2"> 
        <tbody>
            <tr>
                <th>Nom_operation</th>
                <td>{{ chirurgie.nomOperation }}</td>
            </tr>
            <tr>
                <th>Date_chirurgie</th>
                <td>{{ chirurgie.dateChirurgie ? chirurgie.dateChirurgie|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nom_etablissement</th>
                <td>{{ chirurgie.nomEtablissement }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ chirurgie.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chirurgie_index') }}" class="btn btn-info btn-sm"> Retour à la liste</a>

    <a href="{{ path('app_chirurgie_edit', {'id': chirurgie.id}) }}" class="btn btn-info btn-sm">edit</a>

    {{ include('chirurgie/_delete_form.html.twig') }}

{% endblock %}
