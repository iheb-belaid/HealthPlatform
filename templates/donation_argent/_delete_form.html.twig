<form method="post" action="{{ path('app_donation_argent_delete', {'id': donation_argent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donation_argent.id) }}">
    <button class="btn">Delete</button>
</form>
