{% extends 'base.html.twig' %}

{% block title %}DonationArgent{% endblock %}

{% block body %}
  <section class="d-flex justify-content-center align-items-center" style="height: 100vh;">
    <div class="card p-4 shadow" style="width: 100%; max-width: 400px;">
    <h1>DonationArgent</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Email</th>
                <td>{{ donation_argent.Email }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ donation_argent.montant }}</td>
            </tr>
            <tr>
                <th>Methode_paiment</th>
                <td>{{ donation_argent.MethodePaiment }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ donation_argent.Date ? donation_argent.Date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_homepage') }}">valider </a>

    <a href="{{ path('app_donation_argent_edit', {'id': donation_argent.id}) }}">edit</a>

    {{ include('donation_argent/_delete_form.html.twig') }}
{% endblock %}
