
<form method="post" action="{{ path('app_donation_sang_delete', {'id': donation_sang.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donation_sang.id) }}">
    <button class="btn">Delete</button>
</form>

