{% extends 'base.html.twig' %}

{% block title %}Edit DonationSang{% endblock %}

{% block body %}
    <section class="d-flex justify-content-center align-items-center" style="height: 100vh;">
    <div class="card p-4 shadow" style="width: 100%; max-width: 400px;">
    <h1>Edit DonationSang</h1>
  <form method="post" action="{{ path('app_donation_sang_delete', {'id': donation_sang.id}) }}" onsubmit="return confirm('Are you sure you want to Update this item?');">
    {{ include('donation_sang/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_homepage') }}">back </a>

   <!-- {{ include('donation_sang/_delete_form.html.twig') }}-->
{% endblock %}
