<form method="post" action="{{ path('app_suivi_medical_delete', {'id': suivi_medical.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce suivi médical ?');" style="display: inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_medical.id) }}">
    <button type="submit" class="btn btn-danger btn-sm">Supprimer</button>
</form>